apiVersion: v1
kind: Service

metadata:
  name: gateway-service
  namespace: rate-limiter

spec:
  type: NodePort

  selector:
    app: gateway

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30007